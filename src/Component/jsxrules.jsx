const jsxrules = () => {
  return (
    <div>
        <h1>JSX Rules</h1>
        <p>
            JSX only has a single parent element
            <br/>
            JSX must be closed 
            <br/>
            JSX attributes are written using CamelCase (e.g. className instead of Class).
        </p>
    </div>
  )
}

export default jsxrules